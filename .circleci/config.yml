jobs:
  test:
    executor:
      name: node/default
    steps:
      - checkout
      - node/install-packages
      -
        run:
          command: "npm run test"
orbs:
  node: circleci/node@5.0.2
version: "2.1"
workflows:
  test_my_app:
    jobs:
      - test
